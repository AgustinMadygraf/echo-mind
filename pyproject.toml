[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "UP"]
fixable = ["ALL"]

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "basic"
reportUnusedImport = "error"
reportMissingTypeArgument = "warning"
